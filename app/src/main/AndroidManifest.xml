<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vmm.myapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewActivity"
            android:label="@string/title_activity_new" >
        </activity>
        <activity
            android:name=".DemoRecycler"
            android:label="@string/title_activity_demo_recycler" >
        </activity>
        <activity
            android:name=".CardLayout"
            android:label="@string/title_activity_card_layout" >
        </activity>
        <activity
            android:name=".GridActivity"
            android:label="@string/title_activity_grid" >
        </activity>
    </application>

</manifest>
